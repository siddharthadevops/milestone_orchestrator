<!-- GENERATED by the orchestrator from .orchestrator/state.json — do not edit; edits are overwritten at the next gate. -->

# Adjudicated Rejections

Settled findings: re-raising one requires referencing its id with
genuinely new evidence (`contests`).

## [skeleton-codex-r1/SR-SKEL-002]

- unit: skeleton
- severity: P1
- finding: The promised profile conversion cannot represent every valid transitional profile — invalid: the goal promises expressibility of today's profiles (both stored profiles and the in-code seed keep one family per act across rigors) and makes assignment rigor-independent by design; a rigor-varying family is normalized, not preserved, and the skeleton now pins that deterministic rule so conversion never fails or requires hand rewriting.
- rationale: Codex agreed in round 1 (same transcript): 'today's profiles' means the existing profiles requiring conversion, not every future document accepted by the retiring schema; the two stored profiles and the seed are family-consistent; the mandate expressly makes assignment rigor-independent; a deterministic medium-based conversion produces the promised same-name document without failing or requiring a rewrite; asked that the prevention sentence explicitly call this a normalization rather than lossless preservation.
- prevention: implementation/milestones/staffing-router/skeleton.md (Added to the 'Profiles convert once' decision: conversion is a normalization into the document's shape, not a lossless copy; a policy naming no single family (family differing by rigor, or self/opposite relative to a call's origin) converts to one assignment — the family the profile's medium configuration resolves through today's act resolution with no origin (its existing first-family fallback); each rigor's tuning reproduces that rigor's model and effort where the profile staffs that family; the marker shows what ran and the operator edits the document to change it; conversion never fails or skips a valid profile and the operator rewrites none by hand.)

