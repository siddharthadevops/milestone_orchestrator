<!-- GENERATED by the orchestrator from .orchestrator/state.json — do not edit; edits are overwritten at the next gate. -->

# Adjudicated Rejections

Settled findings: re-raising one requires referencing its id with
genuinely new evidence (`contests`).

## [skeleton-codex-r1/SR-SKEL-002]

- unit: skeleton
- severity: P1
- finding: The promised profile conversion cannot represent every valid transitional profile — invalid: the goal promises expressibility of today's profiles (both stored profiles and the in-code seed keep one family per act across rigors) and makes assignment rigor-independent by design; a rigor-varying family is normalized, not preserved, and the skeleton now pins that deterministic rule so conversion never fails or requires hand rewriting.
- rationale: Codex agreed in round 1 (same transcript): 'today's profiles' means the existing profiles requiring conversion, not every future document accepted by the retiring schema; the two stored profiles and the seed are family-consistent; the mandate expressly makes assignment rigor-independent; a deterministic medium-based conversion produces the promised same-name document without failing or requiring a rewrite; asked that the prevention sentence explicitly call this a normalization rather than lossless preservation.
- prevention: implementation/milestones/staffing-router/skeleton.md (Added to the 'Profiles convert once' decision: conversion is a normalization into the document's shape, not a lossless copy; a policy naming no single family (family differing by rigor, or self/opposite relative to a call's origin) converts to one assignment — the family the profile's medium configuration resolves through today's act resolution with no origin (its existing first-family fallback); each rigor's tuning reproduces that rigor's model and effort where the profile staffs that family; the marker shows what ran and the operator edits the document to change it; conversion never fails or skips a valid profile and the operator rewrites none by hand.)

## [slice_impl-02-codex-r2/slice-02a-codex-r1/SD-001]

- unit: slice_impl-02
- severity: P2
- finding: Valid over-ladder ranks make base_staffing partial
- rationale: Codex family consulted (transcript .orchestrator/scratch/slice-02a-SD-001-consult-r1.txt, answer ...-r1-answer.txt); agreement reached in round 1. Codex independently re-ran orchestrator.tests.test_staffing_documents (6/6 pass) and answered all three questions: (1) 'Complete' is explicitly structural — index-1 assignments plus every tuning cell — so 'total lookup' means those cells are present, not that resolution semantics occur; (2) any fix here is out of scope — clamping imports slice 3 saturation, refusing the save contradicts permitted over-ladder ranks, and the typed refusal is coherent; (3) no concrete harmed party exists in slice 2 — no dispatch consumer, conversion cannot generate the case, the operator gets a precise non-mutating error, and slice 3 must process rank and ladder anyway. Verdict: reject as invalid, no behavioural change. Codex's only caveat was that the phrase 'total lookup over a stored document' is misleading terminology that invited the finding and deserves an editorial clarification — taken up as the prevention edit below.
- prevention: orchestrator/staffing.py (The ambiguity was the unqualified word 'total'. base_staffing's docstring now bounds it explicitly — 'TOTAL in exactly the sense completeness is defined: the index-1 assignment and the rigor x slot x role pair are always present ... That is not a promise that every valid document answers' — and the matching test orchestrator/tests/test_staffing_documents.py:test_base_staffing_is_a_total_lookup_over_a_stored_document gained a docstring saying the same and pointing at the saturated-rank test as the documented exception. Wording only; no behaviour, schema, or assertion changed.)

## [slice_impl-03-b-claude-r6/S3B-D3-SHOWN-RECURSION-GAP]

- unit: slice_impl-03-b
- severity: P3
- finding: Claim that `_shown` normalizing `ValueError` but not `RecursionError` lets a raw `RecursionError` escape `resolve`/`session_seats`/`distinct_families_projection`/`read_session`, allegedly newly reachable because fix3 routed `_session_ctx` through `_shown`. The code observation (no `except RecursionError` in `_shown`) is true; the causal claim, the exposure measurement, and the harm claim are not.
- rationale: Codex family consulted (prompt .orchestrator/scratch/S3B-D3-SHOWN-RECURSION-GAP-consult-r1.txt, answer ...-r1-answer.txt); agreement reached in round 1. Codex agreed with REJECT and answered all four questions, while correcting two of my premises. Q1: E1 (identical behaviour at HEAD) disproves new or direct harm for the delta, but exceeds_baseline is strictly measured against the declared contract rather than HEAD, so E1 alone would not absolve a full-artifact defect. Q2: E2 defeats universal totality but not the narrower case — `_shown` could still normalize value-triggered repr recursion under ordinary stack headroom — so rejection must not rest on E2 alone. Q3: the ValueError/RecursionError asymmetry is acceptable as it stands, but 'recursion is not a value property' is too absolute since value depth and ambient headroom jointly cause it; the stronger grounds are unchanged behaviour and no in-scope consumer. Q4: make the docstring-only prevention edit, because 'like every other' invited the finding and the protocol favours clarification when correct behaviour was misread. Codex asked that the edit state that catching recursion locally would not make public operations total against interpreter recursion exhaustion, and avoid claiming value depth is irrelevant; the applied wording does both.
- prevention: orchestrator/staffing.py (Rewrote `_shown`'s docstring (the sentence that invited the finding). Scoped the closing claim from 'the refusal leaves through its own class like every other' to 'THAT refusal leaves through its own class', and added a paragraph naming why the int/str conversion limit is the one limit handled there — it is a property of the value alone — while the recursion limit needs both a deeply nested value and a caller already close to the limit, so catching it locally would normalize one frame of many without making the module's operations total against recursion exhaustion. It also points at `load` and `read_session` as the place stored bytes nested past that limit are normalized, since that is damage in an artifact this module owns.)

## [slice_doc-04-codex-r1/S4-001]

- unit: slice_doc-04
- severity: P2
- finding: Delta-review fallback violates the fixer-family guarantee
- rationale: Codex family consulted (prompt .orchestrator/scratch/S4-001-consult-r1.txt, answer ...-r1-answer.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'REJECT S4-001, with clarification' and answered all five questions. Q1: the skeleton clause is a re-keying, not an unconditional invariant — the milestone's seat choice is limited to assigned review seats, so 'the fixer's family' is the preferred matching seat. Q2: no baseline-preserving alternative exists; resolving `fix 1` uses fix tuning, violates the note's pinned review request, bypasses the review `distinct_families` check, and still cannot survive live edits. Q3: the restart-and-seal argument defeats 'weakening the intended same-family verification'; extra churn or cost from a different reviewer is speculative and bounded by the five-fix delta checkpoint, and final-byte sealing is unchanged. Q4: the behaviour is an observable fallback honouring the operator-authored effective review assignment, not damage — but it corrected my reachability wording to 'not reachable in an unmodified converted document', since later edits remain possible. Q5: it asked me to clarify both the contract row and the acceptance row with one minimal sentence and NOT to repeat the conversion or sealing rationale, which already appears elsewhere; it also corrected my baseline analogue, pointing at the explicit missing-seat-to-index-1 fallback rather than the unreadable-document fallback. I applied its proposed sentence to both rows and adopted both corrections in this account.
- prevention: implementation/milestones/staffing-router/slices/slice-04.md (The old wording led with 'Delta review stays the fixer's family' and then attached the no-matching-seat case as an exception, reading as an unconditional invariant with a hole. Both the Verification Contract row (line 226) and the matching acceptance row (line 249) now state the contract as the codex family worded it: delta review chooses only among assigned `review` seats — the lowest-index seat whose resolved family matches the latest fixer, the lowest assigned seat when none matches, and the marker records the family that ran. No rationale, conversion or sealing text was added; those facts are already elsewhere in the note.)

## [slice_doc-04-claude-r9/S4-DELTA-CLAUDE-001]

- unit: slice_doc-04
- severity: P2
- finding: The delta makes same-family debt rating the normal case but leaves the deferred-debt step message deriving its verifier from `_opposite(family)`, so the run tells the operator a family cleared the debt that never ran.
- rationale: Codex family consulted (prompt .orchestrator/scratch/S4-DELTA-CLAUDE-001-consult-r1.txt, answer ...-r1-answer.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE with rejection, with one qualification: do not claim universal zero harm', and answered all four questions. Q1: yes — the finding's 'only when explicit' condition is true but 'rare opt-in' is false; explicit codex rating is the shipped and stored default, and codex independently confirmed 46/46 codex ratings in this run's state, 29 of them on codex-raised findings. Q2: identical rate holds for the present catalogue — codex re-ran the focused conversion and self-rating tests and confirmed both converted documents preserve codex `classify 1` — while noting the one place the delta does change behaviour is a run whose `reclassifier` override is `opposite`/empty (A2 discards it) or a future opposite-policy profile normalized to a fixed seat. Q3: no declared guarantee covers the step string; the note's guarantees cover the actual call, the marker, the event and the debt entry. Q4: reject as written — the claimed normal-case regression is false and the edge case does not rescue the finding's 'every converted run' argument; no prevention edit, because duplicating machine-specific profile facts into the note invites drift, and rendering the actual `cleared_by` would import a pre-existing HEAD defect into this slice's scope. I have adopted codex's qualification: my incremental_harm above is scoped to reachable runs on the present catalogue rather than claimed universally.

## [slice_doc-04-claude-r17/S4-DELTA-CLAUDE-R2-001]

- unit: slice_doc-04
- severity: P2
- finding: The delta resolves the single-family contradiction by pinning the outage as accepted behaviour: it declares that every run whose available families are one fails at its first review dispatch under the converted `default`, names as the only remedy a one-review-seat document that no surface this slice opens can produce, and re-points the sole homed single-family review test onto that fabricated document — contradicting the skeleton, which cites this exact case as preserved (`skeleton.md:89-91`, the one-seat trivial honouring justified 'as a single-family `families_order` reviews with one family today') and promises the seeded `default` 'staffs every seat as today' (`skeleton.md:145-147`).
- rationale: Codex family consulted (prompt .orchestrator/scratch/S4-DELTA-CLAUDE-R2-001-consult-r1.txt, answer ...-r1-answer.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE — reject; exceeds_baseline=false', stating it independently reproduced the single-family refusal exactly, and answered all five questions. Q1: the binding guarantee is 'a `distinct_families` role with one assigned seat is trivially honoured' (skeleton.md:89); the parenthetical justifies that rule and does not preserve a two-seat default. Q2: skeleton.md:145-147 is seat-content fidelity, delimited by 'exactly, for every seat…' and the seat-by-seat drift alarm. Q3: no exact declared guarantee is violated — codex raised and disposed of the one real counter-candidate itself, goal.md's Compatibility 'next call staffs as it would have', as superseded by A2's explicit whole-compatibility rule, and pointed at goal.md:97 and goal.md:199 as REQUIRING the refusal. Q4: no incremental harm — slices 2/3 provide the refusal and the pre-delta slice-4 contract already applies it at dispatch. Q5: route (a) rejection, no rethink, and explicitly no additional risk or compatibility-consequence entry, since the consequence is already recorded in the core contract at slice-04.md:149 and in the verification row.

## [slice_impl-04-codex-r1/S4A-005]

- unit: slice_impl-04
- severity: P2
- finding: A dispatch-time classifier staffing condition is swallowed and its token overwritten
- rationale: Codex family consulted (same session, prompts ...-r1.txt and -r2.txt, answers ...-r1-answer.txt, -r2-answer.txt); agreement in round 2. Round 1 codex CONFIRMED, granting that classify_evidence made 'lost' overstated but arguing the reason/type/recovery were still wrong and that the guard then probes forever. In round 2 I supplied panel.html:3863-3865 (the evidence is rendered on the failure card beside the reason, verified end to end) and asked codex to choose concretely between (a) the token REPLACING the primary reason, (b) both in the reason, or (c) today's arrangement, and to say whether errclass's never-worsen contract survives. Codex answered (c): "The token is verbatim on the same failure card; replacing or duplicating the primary cause provides no concrete operator benefit"; Q14 "Keep `unknown` and primary-failure recovery. Making classifier staffing govern would suppress useful retry behavior. That precedence change is a design amendment, outside this slice"; Q15 "The never-raise/never-worsen contract survives unchanged"; verdict "S4A-005 - REJECT: the operator sees both causes while recovery remains governed by the real provider failure." Codex also confirmed (Q6) that fixing S4A-002 strengthens rather than weakens this, since the condition now arises only at a real classifier dispatch.
- prevention: orchestrator/driver.py (_classify_failure's docstring now states that a surfaced condition met at the classifier's own dispatch is deliberately not promoted to the run's failure, that the run keeps naming the primary call with its own recovery type, and that the token travels as classify_evidence which the panel prints on the same card.)

## [slice_impl-04-codex-r1/S4A-006]

- unit: slice_impl-04
- severity: P2
- finding: Consultation-time staffing conditions do not enter run recovery
- rationale: Codex family consulted over three rounds (prompts .orchestrator/scratch/S4A-003-005-006-consult-r1.txt, -r2.txt, -r3.txt; answers ...-r1-answer.txt, -r2-answer.txt, -r3-answer.txt); agreement in round 3. Codex held NEED_RETHINK through rounds 1 and 2, arguing the later consultation is a separate physical call with no machine-observed tokenized recovery route, and explicitly told me not to reject. That was decisive until I found the channel I had wrongly assumed absent: the closed `consultation_unavailable` retry envelope and driver._check_worker_blocked's st.fail_run on it. Presented in round 3, codex answered: Q16 "Yes. E13 refutes both statements. The helper alone does not mutate state, but the complete mandated recovery path visibly fails the run"; Q17 missing automatic token propagation "is best-effort provenance, not excess harm; acceptance, staffing, sealing, and results remain correct"; Q18 "No additional machinery is justified. The closed retry envelope plus existing failure/recovery branch is the cheapest sufficient mechanism. Child state writes would violate single-writer ownership"; Q19 "S4A-006 - REJECT (exceeds_baseline: false)." Codex independently ran three focused checks over the retry contract, the prompt and the recovery path.
- prevention: orchestrator/current_model_call.py (consultation_command's docstring now records that this runs in the fixer's subprocess and deliberately writes no run state, and names both routes by which a surfaced condition here still reaches ordinary run recovery: the fixer's `consultation_unavailable` retry envelope, and the driver's own in-process `consult` resolution when it orders the fix.)

## [slice_impl-04-b-codex-r2/S4B-001]

- unit: slice_impl-04-b
- severity: P2
- finding: Review seat selection is frozen before the physical dispatch
- rationale: Codex family consulted across all four rounds of this queue's dialogue (.orchestrator/scratch/S4B-001-002-003-consult-r1.txt through -r4.txt with answers). Agreement on REJECT in every round and never contested: r1 'AGREE-REJECT. The numeric seat is the routed logical call's consumer fact. Physical retries re-resolve staffing behind that seat; they do not reroute the cycle. Missing-seat fallback and G7 make both alleged outcomes permitted.'; r2 'Reject. The seat number remains cycle position while routing consequences stay live. The surviving effects are extra work or a visible/recoverable cap stop, not an invalid seal.'; r3, against my own evidence rather than the earlier summary, 'Reject S4B-001. E1 is sound: the numeric seat is routed cycle position; dispatch staffing, accounting, and cap remain live and family-relative.'; r4 'S4B-001 remains rejected on E1.' Codex volunteered CONFIRM on the two sibling findings in the same answers, so the agreement here is not a reject-everything posture.

## [slice_impl-04-b-codex-r3/S4B-LIVE-001]

- unit: slice_impl-04-b
- severity: P2
- finding: Bounded seat settlement can dispatch a reviewer at the wrong round
- rationale: Codex family consulted (prompt .orchestrator/scratch/S4B-LIVE-001-consult-r1.txt, answer ...-r1-answer.txt plus the subprocess's raw copy ...-r1-answer-raw.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE with REJECTED; exceeds_baseline=false' and answered all five questions. Q1: no transactional binding exists — slice-04.md:245 governs the request derived from a read, live optimistic rewrites govern later resolves, and the milestone provides no atomic mechanism. Q2: no bounded retry closes this — locking, snapshots, generations, router-side derivation and refusal each violate a stated constraint, and raising the retry count only narrows exposure. Q3: given the checked evidence the damage is limited to one call's `step_up` rung with no cap, count, seal or cycle state corrupted, and combined with the explicit optimistic-write posture this falls within baseline (it cautioned that slice-04.md:296 alone is not permission, which I accept — the posture rows carry it). Q4: three precisely timed writes make exposure extremely narrow; rarity alone does not decide validity but supports the intended bounded-residual reading. Q5: make the docstring edit, stating the three-gap floor, the final-family cap and round/answer coherence, and that no FINITE OPTIMISTIC RETRY loop can guarantee family-relative round coherence under continuous writes — explicitly avoiding my broader 'no bounded mechanism whatsoever' claim, since serialization could but would amend the design; leave behaviour unchanged. I wrote the prevention exactly to that correction ('no finite retry closes it... Only freezing the document for the dispatch would, which live uncached reads exclude').
- prevention: orchestrator/driver.py (Extended the `_settled_review_seat` docstring where it already conceded the residual, so the next reader does not re-raise it as an oversight: it now states the three-completed-write floor (one write always settles by the third ask, two leave the last two asks on the same document), that the residual costs at most the rung that one call runs on because the cap is taken at the dispatch on the family returned and no round is recorded from here, and that no finite retry closes it since one more ask only moves the gap the next write lands in — only freezing the document for the dispatch would, which live uncached reads exclude. Docstring only; no behaviour change.)

## [slice_impl-06-codex-r1/S6A-003]

- unit: slice_impl-06
- severity: P2
- finding: Brainstorming erases the exact staffing-condition tokens
- rationale: Codex family consulted (prompt .orchestrator/scratch/S6A-003-consult-r1.txt, answer ...-r1-answer.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE-REJECT — exceeds_baseline=false' and answered all five questions, correcting two of my premises. Q1: no baseline text requires the Brainstorming create response or the stored result to retain the token; the strongest contrary language is skeleton.md:315's 'Milestone law decides what a consumer does with them', and persistence/projection is best-effort with no survival guarantee. Q2: 'surface the exact token at the affected call' means the raise at the pre-provider boundary — 'affected call' is consistently the physical provider call (slice-06.md:170) — and the named strict test observes `.code` there, which codex re-ran independently. Q3: my R3 was too strong — `StaffingConditionError`'s message omits the literal `.code`, so the historical token string is not durable in the log and `/resolve` reports current state rather than the past incident; codex called that weaker observability but still not a violated guarantee. I verified the residue: both messages remain unambiguous in words ('declares distinct_families for ...' versus 'names families ...; this machine has none'), so the finding's own claim that the two cannot be distinguished is false regardless. Q4: 'No fix is required'; the only option offered was optional hardening (an exception note carrying `exc.code`), explicitly not needed, while anything reaching create or the stored result 'necessarily crosses the public mapping or excluded recovery boundary'. Q5: S4A-005 supports consumer-owned recovery vocabulary but does not settle this case, since there the token stayed visible on the failure card; reject on Q1-Q2 rather than on that precedent — so I did not claim it as an adjudicated duplicate.
- prevention: orchestrator/brainstorming_execution.py (The pre-dispatch hook said only that a resolution failure must not manufacture activity, leaving open where the raised condition goes afterwards. Added one sentence at that seam: the raise carrying the exact token is the whole of what this seam owes the condition, which then travels as the discussion's ordinary participant failure, and the router's tokens are not a second stored or public failure language for Brainstorming to speak.)

## [slice_impl-06-codex-r2/S6A-004]

- unit: slice_impl-06
- severity: P1
- finding: The one-shot envelope repair reuses an immutable activity identity and fails after a valid second call
- rationale: Codex family consulted (prompt .orchestrator/scratch/S6A-004-consult-r1.txt, answer ...-r1-answer.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'REJECT; exceeds_baseline=false — the alleged outcome is falsified, the permitted baseline is the observed behavior', and answered all five questions after independently reading the code and running its own probes. Q1: no production-reachable path — coordinator calls always bump via `_before_envelope_repair`, external calls via `retry_external_provider`, production by counting; a hypothetical low-level caller could omit the callback while retaining action context, but no runtime caller does. Q2: yes, the callback completes before the second dispatch and before the repair's context is re-read, in both `exchange_quiescent` and `exchange_control_quiescent`, which share the one `_exchange` path. Q3: yes — codex ran its own production_effect probe and recorded attempts '1 failed' then '2 completed' with distinct event ids. Q4: no new exposure versus HEAD~1; the action and attempt sources are identical and the extraction introduces nothing. Q5: REJECT. Codex confirmed both targeted scenarios and the 27-test pair pass, and noted it did not run the full suite.
- prevention: orchestrator/brainstorming_execution.py (Extended the `_action_context` docstring with the fact the reviewer had to reconstruct from three other modules: the context is read afresh per physical call, so the round stays the action's while the provider attempt moves — an envelope repair bumps the durable attempt before its dispatch (`mark_turn_attempt_envelope_repair`, `retry_external_provider`) and the production branch counts what the ledger already holds, which is why a repair records its own activity instead of reusing the failed call's identity.)

## [slice_impl-06-codex-r4/S6A-CODEX-R3-001]

- unit: slice_impl-06
- severity: P2
- finding: An attached discussion can permanently bind itself to default staffing
- rationale: Codex family consulted (prompt .orchestrator/scratch/S6A-CODEX-R3-001-consult-r1.txt, answer ...-r1-answer.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE-REJECT (exceeds_baseline=false)' and answered all five questions after reading the code and design itself. Q1: no text anywhere requires a live discussion to re-read its owner's binding; the closest, slice-06.md:58, is 'keeps the same live session reference', and 'follows its run's session' governs unmarked pre-cutover records only. Q2: a present-null mark written while the owner is genuinely unbound is FAITHFUL — it records the owner's real absence and fabricates no session id, and skeleton.md:322 defines that absence as visible default@medium. Q3: the harm does not exceed baseline — each call still resolves the fallback visibly, later divergence is permitted because no contract makes an inherited reference follow a future owner rebinding, and default-profile runs do not diverge at all. Q4: no design-preserving fix exists — an owner lookup (even best-effort) violates marked-record authority and, as I showed at brainstorming_lifecycle.py:2673-2681 and service.py:3935-4006, hands a 503-raising lookup a veto over a restart the record can fully staff; propagation rewrites records, blocking creation violates fallback liveness, and indirection adds machinery. Q5: reject.
- prevention: orchestrator/driver.py (Added one paragraph to `Driver._brainstorming_staffing`'s docstring stating that a selection carrying no session is the run's own staffing while it is unbound (not a missing one, since every call it makes meanwhile takes the visible default-document fallback), and that inheritance happens once at creation: the record written is its own authority and a later rebinding does not reach back into a discussion already created. This is the ambiguity that produced the finding — the seam said what it hands over, not what an unbound owner means.)

## [slice_impl-07-codex-r2/S7A-001]

- unit: slice_impl-07
- severity: P2
- finding: Best-effort admission snapshot failure still vetoes the order
- rationale: Codex family consulted (prompt .orchestrator/scratch/S7A-001-consult-r1.txt, answer ...-r1-answer.txt plus raw copy ...-r1-answer-raw.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE-REJECT' and answered all five questions. Q1: 'an admission-time snapshot failure' means failure to derive bookkeeping from production configuration; it does not require surviving arbitrary replacement of the function with raising code. Q2: no production-reachable input is evident — codex qualified that my totality probe is not mathematical proof (exotic dict/str subclasses or resource exhaustion could escape), but real configuration carries ordinary serialized values, for which the implementation is total. Q3: the 500 assertions are correct for an injected programming defect and must NOT be changed to expect 201; the name/docstring were the misleading part, and it prefers splitting or renaming, accepting a clarifying comment as the weaker but valid correction. Q4: a broad `except Exception` at the call site goes beyond the authorized production failure model and would suppress bad refactors and assertion defects — unjustified machinery. Q5: agrees exceeds_baseline=false and disposition `rejected`; the claimed damage requires replacing production code at the seam, and the delivered malformed-configuration case demonstrates the permitted baseline directly. It named textual ambiguity in 'cannot refuse' as the reviewer's strongest point, warranting a test clarification rather than manufactured production reachability.
- prevention: orchestrator/tests/test_staffing_standalone_cutover.py (Kept the recorded test name and both assertions (the name is accurate for the reachable half, and the scope record names it), and removed the ambiguity that produced the finding: the docstring now states that the forbidden refusal is the configuration-caused one and that `worker_staffing` answers every configuration value with `{}` rather than raising, which is why the second half must REPLACE it to see an exception at all; two inline comments mark the first block as the reachable failure that admits and the second as a defect injected into the seam that no caller, configuration or session can produce.)

## [slice_doc-08-codex-r1/S8-001]

- unit: slice_doc-08
- severity: P1
- finding: Core panel controls are incorrectly given a best-effort presentation posture
- rationale: Codex family consulted (prompt .orchestrator/scratch/S8-001-consult-r1.txt, answer ...-r1-answer.txt); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE-REJECT — exceeds_baseline=false' and answered all five questions. Q1: no — the strongest text says a lost/stale projection 'changes no acceptance', so it cannot override explicit Acceptance rows. Q2: yes, with nuance — a successful session GET necessarily contains the warning list, only UI rendering can fail separately, while task/run-detail staffing is independently losable bookkeeping. Q3: no conforming implementation can omit the catalogue, controls or warning; doing so directly fails Acceptance and the named checks. Q4: my proposed prevention adds no machinery and weakens no posture, but codex noted 'their loss' still grammatically reaches catalogue cards and warnings and asked for 'Omission or staleness of derived bookkeeping projections…' followed by an explicit 'successful reads still render the required surfaces' clause. I adopted that wording verbatim in substance. Q5: REJECT — no excess-baseline damage exists.
- prevention: implementation/milestones/staffing-router/slices/slice-08.md (The 'Best-effort — presentation' bullet now says the views gate nothing, that omission or staleness of a derived bookkeeping projection carries no authority over a call, result, seal or accounting, and states explicitly that this bounds authority and not obligation: a successful read still renders the catalogue, session and warning surfaces Acceptance requires, which the checks below pin. Wording follows the consulted family's suggested phrasing so 'their loss' no longer grammatically reaches the required surfaces.)

## [slice_impl-08-c-claude-r6/S8C-DELTA-001]

- unit: slice_impl-08-c
- severity: P3
- finding: The new post-create families comparison in `standaloneStaffingSession` can only ever fire on `str.strip()` normalization, so its sole effect is to block every standalone form for a project whose configured family names carry surrounding whitespace, with a message that misstates the cause.
- rationale: Codex family consulted (prompt `.orchestrator/scratch/S8C-DELTA-001-r2-consult-r1.txt`, answer `...-r1-answer.txt`); clear agreement in round 1, so consultation stopped there. Codex returned 'AGREE-REJECT, narrowly. exceeds_baseline=false. The wording criticism is valid; the claimed behavioral harm is not', said it independently reproduced `['1','2']` versus `['2']`, and answered all five questions. Q1: no — :187's list is not explicitly exhaustive, and a successful create must still satisfy :208's strict accepted-handoff invariant, so 'an HTTP 2xx that normalizes the authoritative family fact is not a conforming handoff'. Q2: the finding's claim is 'unsustainable' — exact matching proves raw and stripped names select different families, and nothing makes the session's normalized spelling authoritative over the consumer-derived order :208 explicitly names. Q3: refusal is the smaller deviation — it leaves an inert session and a recoverable configuration error, while submission silently changes family, model and cost. Q4: keep the exact guard; change only the message, to 'the session records different family names or order than this project's configuration — nothing was submitted', and update the comment to identify store normalization explicitly; a local trim 'would bless the demonstrated divergence and undo the protection'. Q5: removal would reopen codex-S8C-004's post-create equality hole, though the operative reasons are :208 and the reproduced divergent staffing rather than the prior disposition. I applied exactly the Q4 repair and made no other change.
- prevention: orchestrator/static/panel.html (The word ORDER in the thrown message is what led this reviewer to read the guard as an ordering-only check and conclude it prevents nothing. The message now reads 'the session records different family names or order than this project's configuration — nothing was submitted', and the adjacent comment now names store normalization as the divergence that actually occurs, states that family names are matched by exact string, and gives the concrete consequence — a configured " codex " that no ladder can match becomes a stored "codex" that runs, at a different family, model and cost from the project's other standalone work. The guard, its comparison and its focused test are unchanged.)

## [slice_impl-09-codex-r9/S9A-001]

- unit: slice_impl-09
- severity: P1
- finding: UTF-8 encodability narrows the promised shape-only material contract
- rationale: Codex family consulted over two rounds (prompts .orchestrator/scratch/S9A-001-consult-r1.txt, -r2.txt; answers ...-r1-answer.txt, -r2-answer.txt); agreement in round 2. Round 1 codex returned CONFIRM, exceeds_baseline=true, granting that no text says 'every Python str' but holding :94 and :128 jointly admit any string and that encodability is a non-shape condition; it independently reproduced my E1/E2 (uncaught state.save failure with the guard disabled, no upstream repair) and required a fix flipping state and HTTP serialization to ensure_ascii=True. I did not concede or reject: I gathered new evidence and returned. Round 2 supplied E6 (three ensure_ascii=False serializers, including prompts.py:2029 which puts explicit_operator_material_overrides into dispatched prompt text), E7 (measured: this run's own 13.5 MB state carries 13,620 non-ASCII characters, so the flip rewrites every state file's bytes, +68,100 here), E8 (slice-09.md:205 pins document/session stores unchanged and no touch column names state.py or the shared response encoder) and E9 (slice_impl-09-b must carry the same value onto physical router requests). Codex answered all five: Q1 no resolution admitting every str fits this unit's touch set; Q2 'Yes: need_rethink, result_mode=design_amendment — this is a baseline contradiction, not an implementation defect you may repair'; Q3 the real cost is whole-store representational churn and regression exposure; Q4 slice-09.md is the smallest authoritative artifact and option (ii), narrowing the acceptance, is recommended, worded as 'encodable as UTF-8 (therefore containing no unpaired surrogate)' rather than the circular 'this run can store'; Q5 E6(c)/E9 defeat the two-serializer fix and would force option (i) to authorize every prompt and router transport too. I returned need_rethink; the Brainstorming session accepted A5, which adopts exactly codex's option (ii). Against that amended baseline the finding's permitted_baseline premise no longer holds and exceeds_baseline is false.
- prevention: implementation/milestones/staffing-router/slices/slice-09.md (Applied accepted amendment A5 to the one line that caused the finding: the non-goal at :128-131 now reads 'Shape validation therefore checks only string-or-absence, plus that the string is storable as UTF-8; the router remains responsible for meaning.' That 'only' was the licence a reader could use to delete a guard nothing else in the slice authorizes. Per A5 the acceptance at :94-97 takes no edit — A5 narrows what 'a string' means wherever the slice grants it — and :128's own protection is untouched: a rename still never stops a plan or write, membership stays unchecked, and an unknown encodable name is still stored verbatim. Per A5 no production code changed; the accepted direction, previously unpinned, gained two entries in existing test matrices (no new test): a JSON-decoded surrogate PAIR decoding to the single scalar U+10000 joins the plan-side accepted loop in test_slice_contract_takes_a_string_or_nothing_and_never_migrates and the route-side adoption loop in test_material_route_sets_clears_and_respects_task_boundary, both built through JSON decoding so the wire carries the same two escapes the refusals use — pinning the decoded value rather than the spelling. The refused side was already pinned on both surfaces.)

